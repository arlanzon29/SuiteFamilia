/**
 * Decisión de producto explícita: el total del ticket NO interesa. La cifra
 * existe, pero está apagada — lo que la app compara es el precio por unidad de
 * cada artículo, no lo que suma una cesta.
 */
export const MOSTRAR_TOTAL_LISTA = false
